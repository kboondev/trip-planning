# Memory Index

Read this file first, before opening any page below. It stays small by
design as `history/` and `heuristics/` grow — use each entry's
`destination`/`tags` to decide which pages are actually relevant to the
trip at hand, then read only those pages directly. Don't read every file
in `history/` or `heuristics/` for a single trip; that's what this index
is for.

## Preferences

- [Standing preferences](preferences.md) — pace, lodging, dietary/accessibility, transit, budget tendencies. Always read in full; it's one small, single file.

## Heuristics

_(none yet — see `heuristics/README.md` for the page format; entries land here once a human promotes a candidate drafted by `trip-planner-consolidator`)_

## History

_(none yet — see `history/README.md` for the page format; Jarvis adds one line here per completed trip)_
